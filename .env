# FastAPI Application Environment Variables
# Rename this file to .env and populate with your actual or desired values.
# These are loaded by app/config.py using python-dotenv and pydantic-settings.

# Server configuration (uvicorn will use these if passed, or defaults from config.py)
HOST="0.0.0.0"
PORT=8000

# Application settings
APP_NAME="Deepfake Audio Detector"
DEBUG=False # Set to True for development mode (e.g., enables uvicorn reload)

# Model Directory (relative to project root, or absolute path)
# This path is used by app/config.py to locate models.
# In Docker, if models are copied to /app/app/models_onnx, this should be "app/models_onnx".
MODEL_DIR="app/models_onnx"

# Specific model file names (defaults are in config.py, override here if needed)
# CNN_SMALL_MODEL_NAME="CNN_Small.onnx"
# CNN_LARGE_MODEL_NAME="CNN_Large.onnx"

# Audio processing parameters (defaults are in config.py, uncomment to override if they were made configurable via getenv)
# TARGET_SAMPLE_RATE=16000
# CHUNK_DURATION_SECONDS=3.0
# N_MELS=224
# SPECTROGRAM_WIDTH=224
# N_FFT=2048
# HOP_LENGTH=512
# MIN_DB_LEVEL=-80.0
# TOP_DB=80.0

# You can add other environment variables your application might need below.
# Example: API_KEY="your_api_key_here"
